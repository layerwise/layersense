# Layersense Persistence

SQLite persistence package for LayerSense Projects, Scenes, Frames, and Renders.

The controller is the only intended runtime importer. The agent must remain unaware of this package.
